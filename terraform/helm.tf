# helm.tf is intentionally removed.
#
# This assignment uses Azure App Service for Containers (PaaS) as the
# deployment target — no Kubernetes or Helm required.
# See main.tf > azurerm_linux_web_app for the container configuration.
